---
// -----------------------------------------------------------------
// 这是一个极简版的页脚组件。
// 我已经根据您的要求，删除了所有不必要的内容。
// -----------------------------------------------------------------

// 从配置文件中导入作者信息
import { profileConfig } from "../config";

// 动态获取当前年份
const currentYear = new Date().getFullYear();
---

<!-- 
  --- Footer HTML 模板区域 ---
-->
<footer class="text-center text-sm text-gray-500 dark:text-gray-400 py-8">
  
  <!-- 
    【关键修改】
    只保留了最核心的版权信息行。
    - `{currentYear}` 会动态显示当前年份。
    - `{profileConfig.name}` 会动态显示您在 src/config.ts 中设置的名字。
  -->
  <p>
    &copy; {currentYear} {profileConfig.name}
  </p>

  <!-- 
    【已移除】
    - "All Rights Reserved." 文字已被删除。
    - RSS / Atom / Sitemap 链接已被删除。
    - "Powered by Astro & Mizuki..." 这一整行都已被删除。
  -->

</footer>
